import React from 'react';

// TODO: disable No Options message with react-select props, if possible
const SearchFormNoOptionsMessage = () => {
  return null;
};

export default SearchFormNoOptionsMessage;
